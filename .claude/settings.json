{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if echo \"$COMMAND\" | grep -iE \"(UPDATE|DELETE|DROP|INSERT|ALTER|TRUNCATE)\" > /dev/null; then echo \"⛔ BLOCKED: Database modification detected. READ ONLY permission.\" && exit 1; fi'"
          }
        ]
      }
    ]
  }
}
