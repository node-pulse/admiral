# Development Dockerfile for status with hot reload
FROM golang:1.25-alpine

WORKDIR /app

# Install air for hot reloading
RUN go install github.com/air-verse/air@v1.63.0

# Install other dependencies
RUN apk add --no-cache git

# Copy go mod files and download dependencies
COPY go.mod go.sum ./
RUN go mod download

# Copy the rest of the code
COPY . .

# Expose port for status service
EXPOSE 8081

# Use air for hot reloading with status configuration
CMD ["air", "-c", ".air.status.toml"]
