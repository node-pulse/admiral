# Development Dockerfile for submarines-cleaner
# Faster build, includes debugging tools

FROM golang:1.25-alpine

WORKDIR /app

# Install dependencies for development
RUN apk add --no-cache git ca-certificates tzdata

# Copy go mod files
COPY go.mod go.sum ./
RUN go mod download

# Copy source code
COPY . .

# Build the binary (with debug symbols for development)
RUN go build -o /app/cleaner ./cmd/cleaner

# Run binary
ENTRYPOINT ["/app/cleaner"]
